<label class="te-field te-field-color">
    <span class="te-field-label">{{ $label }}</span>
    <input
        type="color"
        class="te-input te-input-color"
        data-key="{{ $dataKey }}"
        value="{{ $value }}"
    >
</label>
